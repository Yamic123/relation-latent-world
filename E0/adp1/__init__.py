"""E0-ADP1 diagnostic experiment: exact alternating sparse-coding EM.

Imported as a top-level package ``adp1`` with ``E0/`` on ``sys.path`` (mirrors the
``diagnostics`` package convention).  The CLI entry point is ``run_adp1.py``.
"""
